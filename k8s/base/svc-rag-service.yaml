apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: genai
  labels:
    app: rag-service
spec:
  selector:
    app: rag-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  type: ClusterIP
