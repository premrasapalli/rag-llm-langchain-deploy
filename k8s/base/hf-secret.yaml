apiVersion: v1
kind: Secret
metadata:
  name: hf-secret
  namespace: genai
type: Opaque
stringData:
  # Hugging Face access token, required ONLY for gated models (e.g. Llama-3,
  # Gemma, Mistral). Leave empty for fully-open models like Qwen.
  hf-token: ""